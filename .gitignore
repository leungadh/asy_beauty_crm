# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Environment / secrets
.env
.env.*

# Logs
*.log

# Future-proofing
node_modules/
dist/
uploads/

# Migration tool — contains service role key, never commit with real credentials
# (commit the blank template, but not after filling in keys)
